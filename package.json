{
  "name": "nexora-academic-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Nexora academic intelligence platform for KITS Singapur CSE",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "start": "python3 run.py",
    "test": "python3 -m unittest discover -s tests -v"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
