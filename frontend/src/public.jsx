import React, {useState} from 'react';
import {useApp, api, Link, navigate, Logo, Icon, Footer, ThemeToggle, Button, Field, ErrorBox, Badge, Avatar, Modal} from './core';

export function PublicNav(){return <nav className="public-nav" aria-label="Main navigation"><Link to="/" aria-label="Nexora home"><Logo/></Link><div className="public-nav-links"><a href="/#platform">The platform</a><a href="/#workflow">How it works</a><Link to="/guide">Project guide</Link></div><div className="nav-actions"><ThemeToggle/><Link to="/student-login" className="nav-signin">Student Login</Link><Link to="/login" className="nav-signin">Faculty Login</Link><Link className="btn btn-primary btn-small" to="/signup">Get started<Icon name="arrow" size={16}/></Link></div></nav>;}

export function Landing(){
 const {login}=useApp();const [busy,setBusy]=useState(false),[error,setError]=useState('');
 async function demo(){setBusy(true);setError('');try{login(await api('/auth/demo',{}));navigate('/app');}catch(e){setError(e.message);}finally{setBusy(false);}}
 return <div className="public-page"><PublicNav/><main>
 <section className="hero"><div className="hero-copy"><div className="hero-eyebrow"><span className="status-dot"/>INTELLIGENCE WITH INTENTION <span>/ PS–014</span></div><h1>Every student.<br/>A clearer path<br/><span>forward.</span></h1><p className="hero-description">A simple academic intelligence platform for KITS Singapur B.Tech CSE under JNTUH R23. Faculty upload marks, attendance and syllabus files; students get clear daily study tasks.</p><div className="hero-actions"><Link to="/signup" className="btn btn-primary btn-large">Build your workspace<Icon name="arrow" size={19}/></Link><Button variant="secondary" busy={busy} onClick={demo} icon="monitor">Explore live demo</Button></div><ErrorBox>{error}</ErrorBox><div className="hero-reassurance"><span><Icon name="check" size={15}/> No API key needed to try</span><span><Icon name="shield" size={15}/> Faculty always in control</span></div><div className="hero-meta"><span className="mono">B.TECH / CSE</span><span className="meta-line"/><span>Built for the people behind the progress.</span></div></div>
 <div className="hero-visual" aria-label="Nexora workspace preview with fictional student data"><div className="visual-top-label"><span className="tiny-square"/> ACADEMIC SUPPORT, CONNECTED.</div><div className="product-window"><div className="window-toolbar"><Logo compact/><span>Faculty workspace</span><span className="preview-label">SAMPLE DATA</span></div><div className="preview-body"><div className="preview-header"><div><div className="preview-overline">YOUR SUPPORT OVERVIEW</div><h3>A little support.<br/>A lasting difference.</h3></div><div className="preview-glyph"><Icon name="chart" size={24}/></div></div><div className="preview-metrics"><div><span>Students</span><strong>12<small>in your cohort</small></strong></div><div><span>Active plans</span><strong>4<small>moving forward</small></strong></div></div><div className="preview-section-title">Study Plan pipeline <span>Faculty review required</span></div><div className="pipeline-preview"><div><span className="pipeline-node"><Icon name="file" size={17}/></span><span>Academic Data</span></div><i/><div><span className="pipeline-node"><Icon name="layers" size={17}/></span><span>Planning</span></div><i/><div><span className="pipeline-node selected"><Icon name="shield" size={17}/></span><span>Review</span></div><i/><div><span className="pipeline-node"><Icon name="chart" size={17}/></span><span>Progress</span></div></div><div className="preview-student"><Avatar name="Aarav Mehta" color={0}/><div><strong>Aarav Mehta</strong><span>Data Structures & Algorithms</span></div><Badge>Active</Badge></div><div className="preview-student"><Avatar name="Diya Nair" color={1}/><div><strong>Diya Nair</strong><span>Database Management Systems</span></div><Badge>Active</Badge></div></div></div><div className="floating-note"><span className="floating-icon"><Icon name="check" size={19}/></span><div><strong>From insight to action.</strong><span>A plan, not just another prediction.</span></div></div><div className="visual-bottom"><span>01 / EVIDENCE</span><span>02 / ACTION</span><span>03 / PROGRESS</span></div></div></section>
 <section className="course-strip" aria-label="CSE curriculum"><span>MADE FOR<br/><strong>COMPUTER SCIENCE.</strong></span><div><span><Icon name="code"/>DSA</span><span><Icon name="layers"/>DBMS</span><span><Icon name="monitor"/>Operating Systems</span><span><Icon name="grid"/>Networks</span><span><Icon name="bolt"/>Machine Learning</span></div></section>
 <section className="public-section" id="platform"><div className="section-intro"><div><span className="eyebrow">A MORE THOUGHTFUL WORKFLOW</span><h2>Beyond identifying the gap.<br/>Help close it.</h2></div><p>One focused space to understand performance, design meaningful study plans, and see what happens next.</p></div><div className="feature-grid">{[
 ['file','Academic Data, not assumptions.','Bring assessment scores, attendance and assignment completion together. Every support signal has a transparent explanation.','01'],
 ['layers','Personalized. Not prescribed.','Use a coordinated planning workflow to draft CSE-specific activities, learning resources and measurable goals.','02'],
 ['refresh','Support that evolves.','Record follow-ups, compare against the baseline, and revise the approach. Keep educators in charge of every decision.','03']
 ].map(([icon,title,desc,n])=><article className="feature-card" key={n}><div className="feature-top"><span className="feature-icon"><Icon name={icon} size={25}/></span><span className="feature-number">{n}</span></div><h3>{title}</h3><p>{desc}</p></article>)}</div></section>
 <section className="workflow-section" id="workflow"><div className="workflow-copy"><span className="eyebrow">SMALL STEPS. MEANINGFUL MOMENTUM.</span><h2>A complete support cycle.<br/>Not a one-off AI response.</h2><p>From the first evidence record to the next faculty check-in, Nexora keeps the context connected.</p><Link to="/guide" className="text-link">See how the agents work<Icon name="arrow" size={17}/></Link><div className="privacy-pill"><Icon name="lock"/><div><strong>Your data. Your choice.</strong><p>Local rules, your Gemini key, or a local Ollama model.</p></div></div></div><ol className="workflow-steps">{[['Understand','Add students and capture academic evidence.'],['Plan & approve','Review an evidence-grounded draft before it becomes active.'],['Implement','Track activities, ownership notes and due dates.'],['Reflect & adapt','Log progress and approve a revised plan when needed.']].map(([h,p],i)=><li key={h}><span>{String(i+1).padStart(2,'0')}</span><div><h3>{h}</h3><p>{p}</p></div><Icon name="chevron" size={19}/></li>)}</ol></section>
 <section className="cta-section"><div><span className="eyebrow">LESS ADMIN. MORE ATTENTION.</span><h2>Make the next step a better one.</h2><p>Your CSE academic support workspace is ready.</p></div><Link to="/signup" className="btn btn-primary btn-large">Start with Nexora<Icon name="arrow"/></Link></section>
 </main><Footer publicPage/></div>;
}

export function Auth({signup=false,student=false}){
  const {login}=useApp();
  const [form,setForm]=useState({name:'',institution:'',email:'',roll:'',password:''}),[busy,setBusy]=useState(false),[error,setError]=useState(''),[show,setShow]=useState(false),[recovery,setRecovery]=useState(false);
  const set=(k,v)=>setForm(f=>({...f,[k]:v}));
  async function submit(e){e.preventDefault();setBusy(true);setError('');try{
    const endpoint=student?'/auth/student-login':'/auth/'+(signup?'signup':'login');
    login(await api(endpoint,student?{roll:form.roll,password:form.password}:form));navigate(student?'/student':'/app');
  }catch(e){setError(e.message)}finally{setBusy(false)}}
  async function demo(){setBusy(true);setError('');try{login(await api('/auth/demo',{}));navigate('/app')}catch(e){setError(e.message)}finally{setBusy(false)}}
  return <div className="auth-page"><header className="auth-nav"><Link to="/" aria-label="Nexora home"><Logo/></Link><ThemeToggle/></header>
    <main className="auth-main"><section className="auth-form-pane"><div className="auth-form-inner"><Link to="/" className="back-link"><Icon name="back" size={16}/> Back to home</Link>
      <span className="eyebrow">{student?'YOUR STUDENT PORTAL':signup?'YOUR FACULTY WORKSPACE':'FACULTY WORKSPACE'}</span>
      <h1>{student?'Welcome back.':signup?'A better starting point.':'Welcome back.'}</h1>
      <p className="auth-subtitle">{student?'See your marks, attendance, study plan and today’s tasks.':signup?'Create a faculty workspace for your CSE cohort.':'Good to see you. Let’s keep the progress going.'}</p>
      <form onSubmit={submit}><ErrorBox>{error}</ErrorBox>
      {student?<Field label="Roll number"><input autoComplete="username" required minLength={2} maxLength={32} value={form.roll} onChange={e=>set('roll',e.target.value.toUpperCase())} placeholder="23CSE101"/></Field>:signup&&<><Field label="Full name"><input autoComplete="name" required minLength={2} maxLength={80} value={form.name} onChange={e=>set('name',e.target.value)} placeholder="Dr. Faculty Name"/></Field><Field label="Institution"><input autoComplete="organization" required minLength={2} maxLength={140} value={form.institution} onChange={e=>set('institution',e.target.value)} placeholder="KITS, Singapur"/></Field>}
      <Field label="Password" hint={student?'Use the password provided by your faculty.':signup?'At least 10 characters, including a letter and a number.':undefined}><span className="password-input"><input type={show?'text':'password'} autoComplete={signup||student?'new-password':'current-password'} required minLength={signup?10:1} maxLength={128} value={form.password} onChange={e=>set('password',e.target.value)} placeholder="Enter your password"/><button type="button" className="icon-btn" onClick={()=>setShow(!show)} aria-label={show?'Hide password':'Show password'}><Icon name="eye" size={18}/></button></span></Field>
      {!signup&&!student&&<button type="button" className="text-link auth-forgot" onClick={()=>setRecovery(true)}>Forgot your password?</button>}
      {signup&&<p className="form-terms">Faculty accounts manage academic records and study plans. <Link to="/privacy">Read the privacy notice.</Link></p>}
      <Button type="submit" busy={busy} className="full-width" icon="arrow">{student?'Sign in to Student Portal':signup?'Create faculty workspace':'Sign in to Nexora'}</Button></form>
      {student?<div className="student-login-hint"><Icon name="info" size={17}/><span>Sample portal: <strong>23CSE101</strong> / <strong>Nexora@1234</strong> when using the sample workspace.</span></div>:
      <><div className="auth-divider"><span>or explore first</span></div><Button variant="secondary" className="full-width" busy={busy} onClick={demo} icon="monitor">Try the sample workspace</Button></>}
      <p className="auth-switch">{student?'Faculty account?':signup?'Already have a workspace?':'Need a faculty workspace?'} <Link to={student?'/login':signup?'/login':'/signup'}>{student?'Faculty Login':signup?'Sign in':'Create an account'}</Link>{student&&<> · <Link to="/student-login">Student Login</Link></>}</p>
    </div></section>
    <aside className="auth-story"><div className="auth-story-top"><span className="mono">{student?'STUDENT ACADEMIC PORTAL':'ACADEMIC STUDY PLAN STUDIO'}</span><span className="outline-chip">{student?'JNTUH R23':'B.TECH CSE'}</span></div>
      <div className="auth-story-body"><div className="story-mark"><Logo compact/></div><h2>{student?<>Know what<br/>to do<br/><span>next.</span></>:<>Behind every<br/>number,<br/><span>a possibility.</span>}</h2><p>{student?'One clear place for your academic progress.': 'Less admin planning.<br/>More time supporting students.'}</p>
      <div className="story-code"><div><span className="code-dot"/> {student?'student.portal':'study plan.workflow'}</div><pre><span className="code-muted">// {student?'Your academic path':'An intentional path forward'}</span>{'\n'}<span className="code-blue">{student?'see':'understand'}</span>{'(performance)\n    .'}<span className="code-blue">{student?'focus':'plan'}</span>{'(what_matters)\n    .'}<span className="code-blue">{student?'check':'review'}</span>{'(progress)\n    .'}<span className="code-blue">{student?'grow':'adapt'}</span>{'(step_by_step);'}</pre><div className="story-code-bottom"><Icon name="shield" size={15}/> {student?'Your view is private by design.':'Faculty judgment stays in the loop.'}</div></div></div>
      <div className="auth-story-bottom">PURPOSE-BUILT FOR COMPUTER SCIENCE EDUCATION<Icon name="arrowUp"/></div></aside></main><Footer publicPage/>
      {recovery&&<Modal title="Recover a local faculty account" onClose={()=>setRecovery(false)}><p>This self-hosted project does not send email reset links. The person running the server can reset a local faculty account from the project folder.</p><pre className="code-block">python scripts/reset_password.py</pre><Button onClick={()=>setRecovery(false)} className="full-width">Got it</Button></Modal>}</div>;
}

const guideSections=[
 ['01','Capture the evidence','Create students individually or import the provided CSV template. Record a course, date, assessment score, attendance and assignment completion. Learning-gap notes help faculty preserve context.'],
 ['02','Generate a draft','The evidence agent validates inputs. The assessment agent calculates an explainable support index. The resource agent retrieves a curated CSE resource. The planner produces structured activities using Local rules, Gemini or Ollama.'],
 ['03','Review before activation','Inspect the evidence citation, resource and agent execution trace. Edit goals, targets and activities, then approve the draft. Only one plan can be active per student and course. This tool never changes grades or sends messages.'],
 ['04','Track implementation','Check off completed activities with faculty notes. Log comparable follow-up assessments, attendance and assignments. Review targets are planning goals, not predicted results.'],
 ['05','Adapt responsibly','The progress evaluator compares the latest check-in with the baseline. A proposed revision remains a draft until faculty approve it. Approval archives the previous active plan while preserving its history.']
];
export function GuideContent(){return <><div className="guide-intro card"><div className="feature-icon"><Icon name="layers" size={26}/></div><div><h2>A bounded, auditable agent workflow.</h2><p>Nexora implements PS-014: Automated Academic Study Plan Planning. It coordinates specialized steps with shared state and a mandatory human review gate, rather than handing decisions to an unrestricted autonomous agent.</p></div></div><div className="guide-grid">{guideSections.map(([n,h,p])=><section className="guide-item card" key={n}><span className="guide-number">{n}</span><div><h3>{h}</h3><p>{p}</p></div></section>)}</div><section className="card methodology"><h2>How the support index works</h2><pre className="code-block">{'0.55 × (100 − assessment)\n+ 0.30 × (100 − attendance)\n+ 0.15 × (100 − assignment completion)'}</pre><p>Each input is a percentage. A student’s index is the equal-weight average of their latest record per observed course. Needs Attention: 45 or above. Keep Improving: 25–44. On Track: below 25. Students without evidence are not scored. These configurable-in-code thresholds are a demonstration heuristic, not a validated predictor or a measure of student ability.</p><p>Progress recommendations use assessment change in percentage points. A decrease or attendance below 60% suggests reviewing support; improvement below 5 points suggests adjusting the approach; otherwise, continue and review. These comparisons do not establish that an study plan caused the change.</p></section></>;}
export function PublicGuide({privacy=false}){return <div className="public-page"><PublicNav/><main className="guide-public"><span className="eyebrow">NEXORA / {privacy?'TRUST & PRIVACY':'PROJECT GUIDE'}</span><h1>{privacy?'Support built on trust.':'From evidence to a way forward.'}</h1><p className="guide-lead">{privacy?'Clear boundaries for a student-centered academic support workflow.':'A practical guide to your B.Tech CSE major project.'}</p>{privacy?<div className="privacy-content card"><h2>Local-first by default</h2><p>Student records and account details are stored in a local SQLite database on the server computer. Each faculty account has a separate workspace. Demo accounts contain fictional records and expire after seven days when the server next starts.</p><h2>Your choice of AI</h2><p>Local rules mode makes no AI network requests and is not generative AI. Ollama runs a model you install on your own machine. Gemini sends minimized course metrics, dates and the learning-gap topic to Google only after you enable consent. Names, roll numbers, email addresses and free-text faculty notes are not included. Do not put personal information in the learning-gap topic.</p><h2>Free-tier limits and data handling</h2><p>Gemini model availability, quotas and free-tier terms can change. Some free-tier data may be used to improve Google’s products. Verify the current terms and use synthetic or appropriately de-identified records. Use a project without paid billing for free-tier experiments. Nexora never silently switches to a paid provider.</p><p><a href="https://ai.google.dev/gemini-api/terms" target="_blank" rel="noreferrer">Google Gemini API terms</a> · <a href="https://ai.google.dev/gemini-api/docs/pricing" target="_blank" rel="noreferrer">Current model pricing</a></p><h2>Credential handling</h2><p>Keys entered in Settings are kept in server memory for the current session, not localStorage or the database. They are forgotten when you sign out or the server restarts. Passwords use salted PBKDF2 hashing. Sessions use HttpOnly cookies and CSRF validation. Code-editor configuration uses a local, untracked .env file assigned to one account email.</p><h2>Human review and limitations</h2><p>This is an engineering project prototype, not an institution-certified student information system. Faculty must verify all plans and follow institutional consent, retention and privacy policies before using real student data. No automated grading, disciplinary decisions, diagnoses or student notifications are implemented. Institution-wide use requires security hardening and evaluation.</p><h2>Removing data</h2><p>A student’s detail page includes a confirmed delete action that removes their evidence, study plan plans and progress records. To remove the entire local installation, stop the server and securely delete its data folder and .env file. Your own exports and backups must be deleted separately.</p></div>:<GuideContent/>}<div className="guide-bottom"><Link to="/signup" className="btn btn-primary">Open your workspace<Icon name="arrow"/></Link><Link to={privacy?'/guide':'/privacy'} className="text-link">{privacy?'Project guide':'Privacy notice'}<Icon name="external" size={16}/></Link></div></main><Footer publicPage/></div>;}
